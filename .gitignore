# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz
./secrets.json
# testing
/coverage
./secrets.json
# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# Large book masters — render to /public/books via scripts/render-book.sh; the
# 562MB PDF and the generated web PDF must never be committed (Git/Vercel limits).
/public/Feather_Fables_Life_Essays.pdf
/feather-fables-web.pdf

# Full-res landscape/travel source JPEGs (181MB). These are optimized and
# uploaded to Convex by scripts/import-landscape.mjs; the app reads from Convex,
# so the originals must never be committed.
/public/landscape/

# typescript
*.tsbuildinfo
next-env.d.ts

# agent / AI-tooling scaffolding (local, not part of the project)
.agents/
.claude/
.superpowers/
AGENTS.md
CLAUDE.md
skills-lock.json
.gstack/
